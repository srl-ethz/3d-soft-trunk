image: registry.ethz.ch/srl/3d-soft-trunk
# pull from Yasu's Docker Hub which has the image generated by the Dockerfile (shift to shared Docker Hub in long term?)
variables:
  GIT_SUBMODULE_STRATEGY: recursive
build:
  script:
    - cmake -DCMAKE_PREFIX_PATH=/opt/drake .
    - make -j$(nproc) # use as many cores as available
